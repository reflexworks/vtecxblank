machine:
  node:
    version: 6.9.5
  timezone: Asia/Tokyo

dependencies:
  override:
    - npm install

deployment:
  production:
    branch: master
    commands:
          - gulp deploy -h https://logioffice.vte.cx -k ${accesstoken}
          - gulp upload -h https://logioffice.vte.cx -k ${accesstoken2}