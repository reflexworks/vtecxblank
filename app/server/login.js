var login = function() { 

/*
	var request = { 'feed' : { entry : [] }};
	var entry = {};
	entry.link = [];
	entry.link[0] = {};
	entry.link[0].___href="/registration";
	entry.link[0].___rel="self";

    request.feed.entry.push(entry);

	ReflexContext.put(request);
*/
	ReflexContext.log("login!!");

};
